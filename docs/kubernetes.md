# kubernetes

Run k8s locally with `kind`

## components
* api server
* etcd
* kubelet
* container runtime
* controller
* scheduler

## kubectl

* get current context: `kubectl conffig current-context`

## configuration

### basic config
required yaml fields:
```
apiVersion:
kind:
metadata:
spce:
```
