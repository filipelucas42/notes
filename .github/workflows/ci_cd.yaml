name: Build and deploy notes
on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master
jobs:
  build:
    runs-on: ubuntu-latest
    container: .
    steps:
      - uses: actions/checkout@v2
      - run: "echo 'Hello, world!'"